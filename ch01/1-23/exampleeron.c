#include <stdio.h>

int main()
{
    
    printf("this is a /* multiline comment */ inside a printf command");
    printf("the comment inside the printf command above is not a comment because it's inside my printf!"); 
}
